---
interface Props {
  body?: string;
  title?: string;
}

const { body, title } = Astro.props;
---

<>
  {title && <h2 class="italic mb-2 font-bold">{title}</h2>}
  {body && <p class="m-0 font-light text-base flex flex-col">{body}</p>}
  <slot />
</>